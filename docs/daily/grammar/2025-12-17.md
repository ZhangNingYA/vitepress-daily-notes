---
layout: doc
date: 2025-12-17
title: 英语五大词性
---

<script setup>
import { ref, computed } from 'vue'

// --- 状态管理区域 ---

// 1. 形容词状态
const mood = ref('happy')
const isHappy = computed(() => mood.value === 'happy')

// 2. 副词状态
const speed = ref(1)
const speedLabel = computed(() => {
  if(speed.value === 0.2) return 'Slowly (慢速)'
  if(speed.value === 1) return 'Normally (正常)'
  return 'Quickly (快速)'
})

// 3. 介词状态
const boxPos = ref('in')
</script>

<style>
/* 动画定义放在最外层 */
@keyframes run {
  0% { transform: translateX(0); }
  50% { transform: translateX(100px); }
  100% { transform: translateX(0); }
}

/* 移动端优先的基础样式优化 */
.grammar-stage {
  background: var(--vp-c-bg-soft);
  border-radius: 12px;
  padding: 20px;
  margin: 20px 0;
  border: 1px solid var(--vp-c-divider);
  overflow: hidden;
  position: relative;
}

.stage-title {
  font-weight: bold;
  opacity: 0.6;
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 15px;
  display: block;
}

/* 列表样式优化 */
.example-list {
  margin-top: 10px;
  padding-left: 20px;
  line-height: 1.6;
}
.example-list li {
  list-style-type: disc;
  margin-bottom: 5px;
}

/* 角色卡片 */
.actor {
  font-size: 3rem;
  transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  display: inline-block;
  cursor: pointer;
}

/* 动词动画类 */
.run-animation {
  animation: run 2s infinite linear;
}

/* 控制按钮组 */
.controls {
  display: flex;
  gap: 10px;
  margin-top: 15px;
  flex-wrap: wrap;
}

.btn {
  padding: 5px 12px;
  border-radius: 20px;
  border: 1px solid var(--vp-c-brand);
  background: transparent;
  color: var(--vp-c-brand);
  font-size: 0.9rem;
  cursor: pointer;
  transition: all 0.2s;
}

.btn.active {
  background: var(--vp-c-brand);
  color: white;
}

/* 介词演示盒子 */
.box-container {
  height: 100px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.the-box {
  width: 60px;
  height: 60px;
  border: 3px solid var(--vp-c-text-1);
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.the-cat {
  font-size: 1.5rem;
  position: absolute;
  transition: all 0.4s ease;
}

/* 位置辅助类 */
.pos-in { opacity: 1; }
.pos-on { transform: translateY(-45px); }
.pos-under { transform: translateY(45px); }
.pos-next { transform: translateX(60px); }

</style>

# 🎬 英语语法片场：好莱坞大片版
> 欢迎来到英语句子的拍摄现场。在这里，语法不是枯燥的规则，而是一部电影的**剧本**。让我们看看谁是主角，谁负责特效。

---

## 1. 名词 (Nouns) —— 舞台上的演员
**Definition:** 任何你能叫出名字的东西（人、地、物、概念）。

想象一下，如果电影里没有人，没有道具，只有一片空白，那是拍不了电影的。名词就是**在此刻登场的实体**。

::: tip 💡 核心记忆
名词 = **What** (是什么)
:::

<div class="grammar-stage">
  <span class="stage-title">Live Demo: 谁在舞台上？</span>
  <div style="text-align: center;">
    <div class="actor">👨‍🚀</div>
    <p>A <strong>Astronaut</strong> (宇航员)</p>
  </div>
</div>

<ul class="example-list">
  <li><strong>专有名词 (Proper Nouns):</strong> 好莱坞巨星，如 <code>Tom Cruise</code> 或 <code>London</code> (必须大写)。</li>
  <li><strong>普通名词 (Common Nouns):</strong> 群演，如 <code>actor</code>, <code>city</code>。</li>
</ul>

---

## 2. 动词 (Verbs) —— 导演的“Action!”
**Definition:** 动作、状态或存在。

只有演员站在那里不动是没法看的。动词赋予了画面**生命**和**时间感**。动词是句子的心脏，没有动词，句子就是死尸。

::: warning ⚡ 能量核心
动词决定了时间（时态）。是“已经发生了”还是“正在发生”？
:::

<div class="grammar-stage">
  <span class="stage-title">Live Demo: 动词控制时间流速</span>
  <div style="display: flex; align-items: center; gap: 20px;">
    <div class="actor run-animation" :style="{ animationDuration: (1/speed) + 's' }">🏃</div>
    <div>
      <p>The boy <strong>runs</strong>.</p>
      <div class="controls">
        <button class="btn" :class="{active: speed===0.2}" @click="speed=0.2">🐢 Walk</button>
        <button class="btn" :class="{active: speed===1}" @click="speed=1">🚶 Run</button>
        <button class="btn" :class="{active: speed===5}" @click="speed=5">🚀 Sprint</button>
      </div>
    </div>
  </div>
</div>

---

## 3. 形容词 (Adjectives) —— 化妆与戏服
**Definition:** 用来修饰名词，给名词“上色”。

名词原本是素颜的，形容词负责给他们穿上戏服，涂上颜色，让他们变得具体。

<ul class="example-list">
  <li><code>The car</code> (这辆车) -> 很无聊。</li>
  <li><code>The <strong>red, broken</strong> car</code> (这辆红色的、坏掉的车) -> 画面感来了。</li>
</ul>

<div class="grammar-stage">
  <span class="stage-title">Live Demo: 换装间</span>
  <div style="text-align: center; transition: all 0.3s;">
    <div class="actor" :style="{ filter: isHappy ? 'none' : 'grayscale(100%) sepia(50%)' }">
      {{ isHappy ? '🤡' : '😭' }}
    </div>
    <p>The <strong>{{ mood }}</strong> clown.</p>
    <div class="controls" style="justify-content: center;">
      <button class="btn" :class="{active: isHappy}" @click="mood='happy'">Happy (开心)</button>
      <button class="btn" :class="{active: !isHappy}" @click="mood='sad'">Sad (悲伤)</button>
    </div>
  </div>
</div>

---

## 4. 副词 (Adverbs) —— 特效与滤镜
**Definition:** 修饰动词、形容词或其他副词。

副词告诉观众动作是**如何 (How)** 发生的。它是电影里的慢动作镜头、快进镜头或者背景音量。

<ul class="example-list">
  <li><strong>Move</strong> (动) -> <code>Move <strong>quietly</strong></code> (悄悄地动)</li>
  <li><strong>Beautiful</strong> (美) -> <code><strong>Extremely</strong> beautiful</code> (极其美)</li>
</ul>

::: details 🔍 怎么辨认副词？
大部分（但不是全部）以 `-ly` 结尾的词通常是副词。就像贴着 "特效组" 的标签。
<br/>
<em>Quickly, Slowly, Happily...</em>
:::

在上面的 **动词 Demo** 中，当你点击按钮改变速度时，你实际上是在改变副词（我在这里使用了 HTML 列表以确保渲染）：

<ul class="example-list">
  <li>He runs <strong>slowly</strong>.</li>
  <li>He runs <strong>quickly</strong>.</li>
</ul>

---

## 5. 介词 (Prepositions) —— 场务的走位图
**Definition:** 表示位置、时间或逻辑关系。

导演大喊：“那只猫要在箱子上面，不是里面！” 这就是介词的作用。它们处理空间几何关系。

<div class="grammar-stage">
  <span class="stage-title">Live Demo: 空间调度</span>
  <div class="box-container">
    <div class="the-box">
      <div class="the-cat" :class="`pos-${boxPos}`">🐱</div>
    </div>
  </div>
  <p style="text-align:center; margin-top: 1rem;">The cat is <strong>{{ boxPos }}</strong> the box.</p>
  <div class="controls" style="justify-content: center;">
    <button class="btn" :class="{active: boxPos==='in'}" @click="boxPos='in'">In (里面)</button>
    <button class="btn" :class="{active: boxPos==='on'}" @click="boxPos='on'">On (上面)</button>
    <button class="btn" :class="{active: boxPos==='under'}" @click="boxPos='under'">Under (下面)</button>
    <button class="btn" :class="{active: boxPos==='next'}" @click="boxPos='next'">Next to (旁边)</button>
  </div>
</div>

---

## 总结：一张图看懂
如果我们把这些元素组合成一个奥斯卡级长镜头：

> The **happy** *(Adj)* **astronaut** *(Noun)* **floats** *(Verb)* **slowly** *(Adv)* **in** *(Prep)* **space** *(Noun)*.

| 词性 | 角色 | 作用 | 关键词 |
| :--- | :--- | :--- | :--- |
| **名词** | 演员 | 实体 | Who/What |
| **动词** | 动作 | 剧情 | Action |
| **形容词**| 戏服 | 装饰演员 | Which one |
| **副词** | 特效 | 调节气氛 | How |
| **介词** | 走位 | 空间关系 | Where/When |
## 🧪 3D 语法实验室

请尝试点击下方的按钮，观察物体的变化。

<GrammarPlanet />